import { SortProductByPrice2Pipe } from './sort-product-by-price2.pipe';

describe('SortProductByPrice2Pipe', () => {
  it('create an instance', () => {
    const pipe = new SortProductByPrice2Pipe();
    expect(pipe).toBeTruthy();
  });
});
